# Server Configuration
PORT=5000
SERVER_HOST=0.0.0.0
SERVER_URL=http://localhost:5000
NODE_ENV=development

# Database Configuration
MONGO_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/your-database

# JWT Configuration
JWT_SECRET=your-jwt-secret-key

# CORS Configuration (comma-separated list of allowed origins)
# For production, specify your exact frontend URLs
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80,http://localhost

# Email Configuration (for reminder service)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-password
